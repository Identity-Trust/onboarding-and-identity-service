spring:
  application:
    name: onboarding-and-identity-service

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health,info

services:
  authentication:
    url: http://localhost:8082